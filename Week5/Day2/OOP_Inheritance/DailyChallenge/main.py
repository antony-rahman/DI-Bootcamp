class Pagination():
    def __init__(self, items,pageSize):
        self.items=items
        items=[]
        self.pageSize= pageSize
        pageSize=10 


def getVisibleItems(): 
    for items in self.PageSize:
     return items