-- select count(actor_id) from actors

-- INSERT INTO actors (first_name, last_name, age, number_oscars)
-- VALUES('Someone', null, null, 7);

-- It won't let me insert null because we specified that the table 
-- doesn't accept null values
